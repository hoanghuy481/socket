/*
 * WebSocketPage Messages
 *
 * This contains all the text for the WebSocketPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WebSocketPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WebSocketPage container!',
  },
});
